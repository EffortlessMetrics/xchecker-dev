--- tests/test_unix_process_termination.rs
+++ tests/test_unix_process_termination.rs
@@ -196,7 +196,7 @@
     use nix::unistd::Pid;

     // Spawn a process that handles SIGTERM gracefully
-    let mut cmd = CommandSpec::new("sh").arg("-c").arg("trap 'exit 0' TERM; while true; do sleep 1; done").to_tokio_command();
+    let mut cmd = CommandSpec::new("sh").arg("-c").arg("trap 'exit 0' TERM; sleep 30").to_tokio_command();
     cmd.stdin(Stdio::null())
         .stdout(Stdio::null())
         .stderr(Stdio::null());
