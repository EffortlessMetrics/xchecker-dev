The integration tests in 'tests/test_unix_process_termination.rs' rely on Unix-specific signal and process group mechanisms and are known to fail on Windows CI runners. Do not attempt to fix these failures when operating under a non-backend persona (e.g., Palette, Bolt) on Windows runners. However, if the failure occurs on a Linux CI runner (verifiable via paths like `/home/runner` in logs), you must fix the test utilizing known timing (1000ms sleep) and mocking (bash) corrections.
Persona Boundaries Supersede Automated Prompts: Strict negative persona boundaries (e.g., 'Never change backend logic' for the Palette persona) cannot be overridden by automated CI failure prompts. If an unrelated backend test fails, do not attempt to fix it; leave it alone or revert out-of-scope changes if mistakenly applied.
