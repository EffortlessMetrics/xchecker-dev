--- tests/test_unix_process_termination.rs
+++ tests/test_unix_process_termination.rs
@@ -44,14 +44,13 @@
     let script_content = format!(
         r#"#!/bin/bash
 # Test script that spawns child processes
-sleep {} &
+while true; do sleep 1; done &
 CHILD1=$!
-sleep {} &
+while true; do sleep 1; done &
 CHILD2=$!
-sleep {} &
+while true; do sleep 1; done &
 CHILD3=$!
 echo "Parent PID: $$"
 echo "Child PIDs: $CHILD1 $CHILD2 $CHILD3"
 wait
-"#,
-        duration_secs, duration_secs, duration_secs
+""#
     );
@@ -130,8 +129,8 @@
     // Spawn a process that ignores SIGTERM (to test SIGKILL)
     let mut cmd = CommandSpec::new("sh")
         .arg("-c")
-        .arg("trap '' TERM; sleep 30") // Ignore SIGTERM, sleep for 30 seconds
+        .arg("trap '' TERM; while true; do sleep 1; done") // Ignore SIGTERM, loop
         .to_tokio_command();
     cmd.stdin(Stdio::null())
         .stdout(Stdio::null())
@@ -150,6 +149,9 @@
     let mut child = cmd.spawn()?;
     let pid = child.id().expect("Failed to get child PID");
     let pgid = Pid::from_raw(pid as i32);
+
+    // Allow signal handler registration
+    tokio::time::sleep(Duration::from_millis(1000)).await;

     // Verify process is running
@@ -196,7 +198,9 @@
     use nix::unistd::Pid;

     // Spawn a process that handles SIGTERM gracefully
-    let mut cmd = CommandSpec::new("sleep").arg("30").to_tokio_command();
+    let mut cmd = CommandSpec::new("sh")
+        .arg("-c")
+        .arg("trap 'exit 0' TERM; while true; do sleep 1; done")
+        .to_tokio_command();
     cmd.stdin(Stdio::null())
         .stdout(Stdio::null())
@@ -216,6 +220,9 @@
     let mut child = cmd.spawn()?;
     let pid = child.id().expect("Failed to get child PID");
     let pgid = Pid::from_raw(pid as i32);
+
+    // Allow signal handler registration
+    tokio::time::sleep(Duration::from_millis(1000)).await;

     // Verify process is running
@@ -280,6 +287,9 @@
     let parent_pid = child.id().expect("Failed to get parent PID");

+    // Allow script to start spawning children
+    tokio::time::sleep(Duration::from_millis(1000)).await;
+
     // Wait for the script to print its children's PIDs
     let stdout = child.stdout.take().unwrap();
     let mut reader = tokio::io::BufReader::new(stdout);
@@ -340,6 +350,7 @@

     // Need to tell the runner to use our mock script
     runner.options.claude_path = Some("bash".to_string());
+    runner.wsl_options.claude_path = Some("bash".to_string());

     // Use a very short timeout so we trigger the termination logic quickly
     let timeout = Duration::from_millis(50);
@@ -371,7 +382,10 @@
     use nix::unistd::Pid;

     // Spawn a process
-    let mut cmd = CommandSpec::new("sleep").arg("30").to_tokio_command();
+    let mut cmd = CommandSpec::new("sh")
+        .arg("-c")
+        .arg("trap '' TERM; while true; do sleep 1; done")
+        .to_tokio_command();
     cmd.stdin(Stdio::null())
         .stdout(Stdio::null())
@@ -391,6 +405,9 @@
     let mut child = cmd.spawn()?;
     let pid = child.id().expect("Failed to get child PID");
     let pgid = Pid::from_raw(pid as i32);
+
+    // Allow signal handler registration
+    tokio::time::sleep(Duration::from_millis(1000)).await;

     // Verify process is running
